name: Deploy
on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Check out mothertongues
        uses: actions/checkout@v7
        with:
          repository: MotherTongues/mothertongues
          submodules: recursive
          path: mothertongues
      - name: Use Node.js 22
        uses: actions/setup-node@v7
        with:
          node-version: '22'
      - name: Install uv
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9
        with:
          python-version: "3.11"
      - name: Install NPM dependencies
        run: |
          # The default config is for the monorepo, while this is for standalone
          cd mothertongues/mothertongues-UI/ && npm install
      - name: Install python dependencies
        run: |
          # The default config is for the monorepo, while this is for standalone
          # TODO: eventually g2p should be removed
          cd mothertongues && uv sync && uv run pip install g2p
      - name: Compile dictionary data
        run: |
          cd mothertongues && uv run mothertongues export ../config.mtd.json mothertongues-UI/packages/mtd-mobile-ui/src/assets/
      - name: Copy audio assets
        run: |
          # data.csv's Audio/Example Audio columns reference paths like
          # assets/audio/<file>.mp3 (resolved at runtime as a URL relative to
          # the deployed page), so the files need to land at that exact path
          # inside the Angular app's own assets directory before the build.
          mkdir -p mothertongues/mothertongues-UI/packages/mtd-mobile-ui/src/assets/audio
          cp audio/*.mp3 mothertongues/mothertongues-UI/packages/mtd-mobile-ui/src/assets/audio/
      - name: Test
        run: |
          cd mothertongues/mothertongues-UI && npx nx test search
      - name: Build
        run: |
          cd mothertongues/mothertongues-UI && npx nx build search
          npx nx build mtd-mobile-ui --base-href=/mtd-starter/ --configuration=production
      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          clean: true
          branch: gh-pages # The branch the action should deploy to.
          folder: mothertongues/mothertongues-UI/dist/packages/mtd-mobile-ui # The folder the action should deploy.
